name: hello-world

on: push

jobs:
  hello-world-job:

    runs-on: ubuntu-latest

    steps:

      - name: checks out repository code.
        uses: actions/checkout@v4
      - run: echo "Hello, World!"